#include <stdio.h>
#include <conio.h>

void main() {
    float base, height, area;
    base = 4;
    height = 4;
    area = base*height/2;
    printf("The area of the triangle is %.2lf", area);
    getch();
}